# Moving Fast: Agentic systems using LLMs

To run the presentation install the packages and start a simple server using `npm`:

Install:

```shell
npm install
```

Start

```shell
npm start
```

The presentation was created using [reveal.js](https://revealjs.com)
